{
  "name": "mnist-wgan-gp-browser",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync-ui": "node scripts/prepare-index.mjs",
    "dev": "npm run sync-ui && vite --config vite.config.js",
    "build": "npm run sync-ui && vite build --config vite.config.js",
    "preview": "vite preview --config vite.config.js",
    "test": "node --test tests/*.test.js",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "onnxruntime-web": "1.27.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "vite": "8.1.4"
  }
}
